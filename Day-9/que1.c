#include<stdio.h>

int main(){

    int a ;

    printf("Enter any value\n : ");
    scanf("%d",&a);

  
  

  (a % 2 == 0) ? printf("The number is even") : printf("The number is odd");

    // (b == 0) ? printf("the number is even") : ("The number is odd");
}